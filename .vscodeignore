.vscode/**
.vscode-test/**
tools/**
.spinney/**
.agent-harness/**
.github/**
src/**
node_modules/**
.gitignore
.gitattributes
AGENTS.md
tsconfig.json
**/*.map
**/*.ts
build-deploy.ps1
docs/**
# Repository art for the README (screenshots). It never ships: the Marketplace reads
# the README from the repository, and vsce rewrites a relative image path to its raw
# URL, so the file is fetched, not packaged.
assets/**
*.vsix
